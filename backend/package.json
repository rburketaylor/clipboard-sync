{
  "name": "clipboard-sync-backend",
  "version": "1.0.0",
  "description": "Python backend API for clipboard synchronization - Node.js tooling",
  "scripts": {
    "install-python-deps": "pip install -r requirements.txt",
    "dev": "python -m uvicorn app:app --reload --host 0.0.0.0 --port 8000",
    "test": "python -m pytest",
    "docker-build": "docker build -t clipboard-sync-backend .",
    "docker-run": "docker-compose up"
  },
  "keywords": ["clipboard", "sync", "fastapi", "backend"],
  "author": "",
  "license": "MIT"
}